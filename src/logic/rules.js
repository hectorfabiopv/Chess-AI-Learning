// src/logic/rules.js
export function isCheckmate(board, currentPlayer) {
  // Implementa la lógica para verificar si el rey está en jaque mate
  return false; // Temporal
}

export function isStalemate(board, currentPlayer) {
  // Lógica para verificar si hay un empate
  return false; // Temporal
}
